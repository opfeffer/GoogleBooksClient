import Foundation

public enum PrintTypes: String {
    case all
    case books
    case magazines
}

public enum Projection: String {
    case full
    case lite
}

public enum Sorting: String {
    case relevance
    case newest
}
